$

$
	integer numba;
	real tupac;
	boolean boohoo;
	integer fan1, fan_2, fan3_;
	real theRealest, alsoTupac, eminem;
	boolean booyou,boomee,boothee;
$
	scan (boohoo, eminem);
	
	if (boohoo == true)
	tupac = -1;
	endif
	
	if (tupac == -1)
		if (eminem == true)
		{
			tupac = tupac + 3;
			theRealest = false;
		}
		else
			alsoTupac = -false;
		endif
	endif
$